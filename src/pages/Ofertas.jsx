import{useEffect,useState}from'react';import ProductCard from'../components/ProductCard';import{getProducts}from'../services/products';import'../styles/catalogo.css';export default function Ofertas(){const[p,setP]=useState([]);useEffect(()=>{getProducts().then(setP)},[]);return <main className="catalog-page"><section className="catalog-hero sale-hero"><div className="container"><span>POR TEMPO LIMITADO</span><h1>Ofertas especiais</h1><p>Os melhores achados com preços que merecem atenção.</p></div></section><section className="container section"><div className="products-grid">{p.map(x=><ProductCard key={x.id} product={x}/>)}</div></section></main>}
